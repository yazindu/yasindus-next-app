import React from 'react';
import AddToCart from "@/app/components/AddToCart";

const ProductCard = () => {
    return (
        <div>
            <AddToCart/>
        </div>
    );
};

export default ProductCard;